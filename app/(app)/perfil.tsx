import { View } from "react-native";
import Header from "../../components/UI/Header";

const Perfil = () => {
  return (
    <View className="flex-1">
      <Header title="Perfil" showBackArrow />
      {/* Contenido */}
    </View>
  );
}

export default Perfil;